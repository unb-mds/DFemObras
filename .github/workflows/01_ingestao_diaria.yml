name: 01 - Ingestão Diária de Dados (Obras e Financeiro)

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch: 

jobs:
  ingestion:
    runs-on: ubuntu-latest
    timeout-minutes: 10 

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install duckdb pandas requests python-dotenv

      - name: Rodar Ingestão Física (Obras)
        env:
          MOTHERDUCK_TOKEN: ${{ secrets.MOTHERDUCK_TOKEN }}
        run: python data_eng/ingestion/ingestao_obras.py

      - name: Rodar Ingestão Financeira (Empenhos)
        env:
          MOTHERDUCK_TOKEN: ${{ secrets.MOTHERDUCK_TOKEN }}
        run: python data_eng/ingestion/ingestao_financeira.py